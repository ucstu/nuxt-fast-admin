import { defineAppConfig } from "#imports";

export default defineAppConfig({
  fastNav: {
    menus: [
      {
        name: "for",
        title: "for",
      },
      {
        name: "bar",
        title: "bar",
      },
    ],
  },
});
